package com.example.ambassadorpass.viewmodel

import androidx.lifecycle.LiveData
import androidx.lifecycle.ViewModel
import com.example.ambassadorpass.Model.Ambassador
import com.example.ambassadorpass.repository.UserRepository

class DashboardViewModel(private val repository: UserRepository) : ViewModel() {
    // Similar approach can be used to get ambassador data
}
